[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci --prefer-offline"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node dist/index.cjs"

[variables]
NODE_ENV = "production"
NODE_OPTIONS = "--max-old-space-size=2048"
